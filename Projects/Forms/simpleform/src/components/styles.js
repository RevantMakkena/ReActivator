import {makeStyles} from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  fields: {
    marginTop: "20px",
    width: "100%",
  },
  submitFields: {
    marginTop: "10px",
    width: "50%",
  },
}));

export default useStyles;
